# GraphRAG 현재 단계 분석 보고서 (v2)

> 기준일: 2026-03-12 | 분석 기준: main 브랜치 (v6.0 OWL Reasoner 포함)
> 프롬프트: `docs/prompts/현재_단계_분석_및_개선_프롬프트.md` (개선판)

### 규모 (스크립트 측정)
- Backend 소스: ~14,964 LOC, 96개 파일
- 테스트: 39개 파일, ~508개 함수 (597 collected, parametrize 포함)
- API 엔드포인트: 36개 (14개 라우터)
- Frontend 페이지: 6개

---

## Part 1: 현황 분석 레이더 차트

```
축                    | 점수 | 등급 | 확신도
─────────────────────|──────|────|────────
아키텍처 건전성        | 78   | B  | HIGH
데이터 파이프라인      | 82   | A  | HIGH
쿼리 품질             | 76   | B  | HIGH
테스트 & 품질 보증     | 72   | B  | HIGH
프로덕션 준비도        | 73   | B  | HIGH
사용자 경험           | 71   | B  | HIGH
확장성 & 유지보수      | 77   | B  | HIGH
─────────────────────|──────|────|────────
종합 (가중 평균)       | 76   | B  |
```

가중 평균 산출: (78×1.0 + 82×1.2 + 76×1.2 + 72×1.0 + 73×1.3 + 71×0.8 + 77×0.8) ÷ 7.3 = **75.7 → 76**

---

## Part 2: 축별 상세 분석

---

### 축 1: 아키텍처 건전성 — 78점 (B) | HIGH

**1. 현재 수준**
14개 라우터 → 서비스 → DB 3계층이 일관 적용됨. 라우터가 얇고 비즈니스 로직은 서비스에 집중. 17개 커스텀 Exception + graceful degradation이 전 레이어에 관통. LLM 프로바이더 추상 레이어가 없어 if/else 분기가 산재.

**2. 근거**
- `routers/ontology.py:15-27` — 라우터 13줄, `generate_ontology()` 위임만 수행
- `routers/query.py:16-23` — 라우터 8줄, `run_query()` 위임
- `ontology/llm_enricher.py:17-66` — `_call_llm_enricher()` OpenAI→Anthropic if/else 폴백
- `query/router_llm.py:117-167` — 동일 if/else 패턴 중복
- `config.py:20-85` — 단일 Settings 클래스, `.env` 오버라이드, 하드코딩 시크릿 없음
- `kg_builder/service.py:27` — `_jobs: dict` 인메모리 상태 (PG 폴백 존재)
- `csv_import/parser.py:16` — `_csv_sessions: dict` 인메모리 (TTL 3600s, MAX 50)
- `main.py:143-165` — shutdown 훅에서 드라이버/풀 정리

**3. 강점**
- 라우터가 얇음 — 비즈니스 로직 0, 요청/응답 변환만
- `pydantic-settings` 기반 환경 설정 일원화
- Non-fatal graceful degradation 패턴 일관 적용
- 인메모리 상태에 TTL/pruning/PG 폴백 구현 (`_prune_completed_jobs` service.py:46-58)

**4. 약점**
- LLM 프로바이더: OpenAI→Anthropic if/else 분기가 `llm_enricher.py`, `router_llm.py`, `local_search.py` 3곳에 중복
- DI 부재: 서비스가 `get_driver()`, `_get_pool()` 직접 호출 — 테스트 시 monkeypatch 필수
- 인메모리 `_jobs` 다중 워커 불가 (PG 폴백은 있으나 primary가 dict)

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| SHOULD | LLM 프로바이더 Strategy 패턴 (`llm/provider.py` 신규) | `llm_enricher.py:17-66`, `router_llm.py:117-167` |
| SHOULD | `_jobs` primary를 PG로 전환 | `kg_builder/service.py:27` |
| COULD | FastAPI `Depends()` 기반 DB 클라이언트 DI | `neo4j_client.py`, `pg_client.py` |

---

### 축 2: 데이터 파이프라인 완성도 — 82점 (A) | HIGH

**1. 현재 수준**
DDL→FK 규칙→LLM 보강→OWL 추론→KG 빌드→물리화 5단계 풀 파이프라인 동작. MERGE 기반 멱등 로드, 단계별 try-catch-log-fallback 일관 적용. 비원자적 다단계 로드와 최소한의 retry가 약점.

**2. 근거**
- `ontology/pipeline.py:35-99` — 3-stage 오케스트레이션 (FK→LLM→OWL), 각 단계 non-fatal
- `kg_builder/loader.py:87-114` — MERGE 사용 (CREATE 아님), 노드+관계 모두 멱등
- `kg_builder/loader.py:324-390` — `execute_write()` 단위 트랜잭션, 노드→관계 별도 트랜잭션
- `kg_builder/service.py:253-279` — 빌드 실패 시 `MATCH (n) DETACH DELETE n` 정리
- `kg_builder/loader.py:36-57` — `_wait_for_neo4j()` 연결 재시도 (2초 간격)
- `document/embedder.py:31-55` — OpenAI→로컬→제로벡터 3단 폴백

**3. 강점**
- MERGE 기반 완전 멱등 (노드 + 관계)
- 5단계 모든 스테이지에 non-fatal fallback
- 임베딩 3단 폴백 (OpenAI→sentence-transformers→제로벡터)
- CSV/YAML/문서 파이프라인 모두 동일한 try-catch-log 패턴

**4. 약점**
- 노드 로드와 관계 로드가 별도 트랜잭션 — 관계 실패 시 고아 노드 잔존
- 운영 중 retry 없음 (연결 retry만 존재, MERGE 실패 시 재시도 없음)
- `tenant_id=None`일 때 `MATCH (n) DETACH DELETE n` — 전체 테넌트 데이터 삭제 위험

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| MUST | tenant_id=None 방어 (기본값 할당 or 전용 라벨) | `kg_builder/service.py`, `loader.py:330` |
| SHOULD | 노드+관계 단일 트랜잭션 래핑 | `kg_builder/loader.py:324-390` |
| COULD | MERGE 실패 retry (최대 2회) | `kg_builder/loader.py` |

---

### 축 3: 쿼리 품질 — 76점 (B) | HIGH

**1. 현재 수준**
Mode A (28 템플릿), Mode B (서브그래프 LLM), Mode C (벡터+KG 하이브리드) 3모드. 20개 규칙 패턴 + LLM 폴백 라우터. 102개 골든 QA 페어. 동의어 미지원, 일반 쿼리 캐시 부재가 약점.

**2. 근거**
- `query/router_rules.py:28-245` — 20개 regex 패턴 (Q1-Q20), 한국어+영어 이중
- `query/template_registry.py:218-471` — 28개 템플릿 (14 traverse + 14 aggregate)
- `query/router_llm.py:19-48` — 시스템 프롬프트에 스키마+OWL+_CLOSURE 정보 주입
- `query/pipeline.py:47-53` — 데모 캐시만 존재 (5개 Q&A), 일반 캐시 없음
- `query/reranker.py:46-51` — 2단계: 키워드(항상) + LLM(OpenAI 키 있을 때), non-fatal
- `query/slot_filler.py:14-61` — 화이트리스트 기반 Cypher 인젝션 방지
- `evaluation/qa_golden.py` — 50 structured + `qa_golden_unstructured.py` 52 unstructured = 102 pairs

**3. 강점**
- 3단계 라우팅: 규칙(무료) → LLM(유료) → unsupported (비용 최적)
- LLM 리랭커 실제 동작 (이전 분석에서 스텁으로 오인 — 실제로는 active)
- 102개 골든 QA 페어로 평가 체계 완비
- Cypher 인젝션 방지: 레이블/관계/속성/연산자 화이트리스트 + max_depth 1-10 범위 제한
- 엔터티 추출이 동적 (Neo4j에서 실시간 로드, TTL 300초 캐시)

**4. 약점**
- 20개 규칙 패턴이 정확한 키워드 매칭 — 동의어/유사 표현 미지원
- 일반 쿼리 결과 캐시 없음 (매번 Neo4j 재실행)
- 도메인 특화 템플릿 14개가 이커머스 중심 — 타 도메인 커버리지 부족
- 규칙 패턴에 기본 plural 처리(`_depluralize`)만 — NLP 기반 정규화 없음

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| SHOULD | 동의어 사전 (synonym map) + 규칙 라우터 확장 | `query/router_rules.py` |
| SHOULD | 일반 쿼리 결과 LRU 캐시 (TTL 60초) | `query/pipeline.py` |
| COULD | 스키마 인식 동적 Cypher 생성 | `query/cypher_generator.py` (신규) |

---

### 축 4: 테스트 & 품질 보증 — 72점 (B) | HIGH

**1. 현재 수준**
39개 테스트 파일 중 24개 활성 (508 함수, 597 collected). conftest.py 공유 fixture, Neo4j/PG/LLM 전부 모킹으로 CI 독립 실행. 14개 스텁 파일, coverage 도구 없음, 프론트엔드 테스트 0개가 약점.

**2. 근거**
- `tests/conftest.py:24-66` — `ecommerce_erd`, `app`, `client` 등 7개 공유 fixture
- `tests/test_query.py:80-95` — `pytest.raises(QueryRoutingError)` Cypher 인젝션 테스트
- `tests/test_e2e_integration.py:67-236` — 6개 E2E (DDL→온톨로지→승인→KG→Q&A)
- `.github/workflows/ci.yml:10-27` — `pytest tests/ --tb=short -q` (coverage 미측정)
- `frontend/package.json` — test 스크립트 없음, vitest 미설치
- 14개 스텁 파일 (`test_wisdom.py`, `test_vector_search.py`, `test_reranker.py` 등) — 0개 함수

**3. 강점**
- conftest fixture 패턴으로 테스트 간 일관성 유지
- 86개 에러/엣지 케이스 테스트 (pytest.raises 4, assert None 40+, 상태코드 22+)
- 100% 모킹 — CI에서 외부 의존 없이 실행
- CI 존재 (GitHub Actions: backend-test + frontend-build + docker-build)

**4. 약점**
- 14개 스텁 테스트 파일이 CI에서 경고 없이 통과
- `pytest-cov` 미설치 — 커버리지 측정/임계값 없음
- 프론트엔드 테스트 0개 (Vitest, Testing Library 미설치)
- E2E에서 Neo4j/LLM 실패 시나리오 미검증

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| MUST | `pytest-cov` 추가 + CI에서 `--cov=app --cov-fail-under=60` | `requirements.txt`, `.github/workflows/ci.yml:27` |
| SHOULD | 14개 스텁 파일 정리 (구현 or 삭제) | `tests/test_wisdom.py` 등 |
| SHOULD | 프론트엔드 테스트 (Vitest + Testing Library) | `frontend/vitest.config.ts` (신규) |
| COULD | E2E 실패 시나리오 (Neo4j down, LLM rate limit) | `tests/test_e2e_integration.py` |

---

### 축 5: 프로덕션 준비도 — 73점 (B) | HIGH

**1. 현재 수준**
Dockerfile(backend+frontend), docker-compose(전체 스택), CI/CD(GitHub Actions), rate limiting, 3모드 인증, 구조화된 JSON 로깅, Neo4j/PG 핑 헬스체크 모두 구현. 백업/복구 전략 문서화와 분산 rate limiting이 미비.

**2. 근거**
- `logging_config.py:20-33` — production 모드: JSON 구조화 로깅 (ts, level, logger, message, extra)
- `auth.py:24-116` — ASGI 미들웨어, 3모드 (no-auth / single-key / multi-tenant)
- `rate_limit.py:18-49` — 슬라이딩 윈도우 60req/60s, 429+Retry-After 헤더
- `routers/health.py:16-54` — Neo4j `verify_connectivity()` + PG `SELECT 1` + 지연 시간 측정
- `backend/Dockerfile:1-24` — python:3.11-slim, healthcheck 포함
- `docker-compose.yml:1-92` — Neo4j 5 + PG 16(pgvector) + backend + frontend, 모두 health check
- `.github/workflows/ci.yml:1-60` — backend-test → frontend-build → docker-build 3단계

**3. 강점**
- docker-compose 한 줄로 전체 스택 기동 (Neo4j+PG+backend+frontend)
- 구조화된 JSON 로깅 (production 모드 자동 전환)
- ASGI 레벨 인증 미들웨어 (BaseHTTPMiddleware 회피, multipart 호환)
- Cypher 인젝션 화이트리스트 + PG 파라미터 바인딩
- CI에서 lint + test + docker build 자동화

**4. 약점**
- rate limiter가 인메모리 — 다중 컨테이너 배포 시 공유 불가
- 백업/복구 전략 문서 없음 (docker volume만)
- CI에서 커버리지 미측정 + 임계값 없음
- JWT/OAuth 미지원 (API 키 수준)

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| SHOULD | 백업/복구 문서 (pg_dump + neo4j-admin dump) | `docs/BACKUP.md` (신규) |
| SHOULD | CI 커버리지 측정 + 임계값 설정 | `.github/workflows/ci.yml` |
| COULD | Redis 기반 분산 rate limiting | `rate_limit.py` |
| COULD | JWT 인증 (API 키와 병행) | `auth.py` |

---

### 축 6: 사용자 경험 (UX) — 71점 (B) | HIGH

**1. 현재 수준**
6개 페이지 모두 에러 상태/로딩 상태 구현. QueryPage는 접근성 우수 (role="log", role="radio", aria-checked, aria-busy). 중앙 Toast 없이 인라인 alert 사용, 일부 페이지 모바일 미대응.

**2. 근거**
- `UploadPage.tsx:534-538` — `role="alert"` destructive 스타일 에러 표시
- `ReviewPage.tsx:370-374` — 성공 메시지 5초 자동 소멸 (`SUCCESS_MSG_TIMEOUT_MS`)
- `QueryPage.tsx:146-195` — `role="radio" aria-checked` 모드 토글, `role="log" aria-live="polite"` 메시지 영역
- `ExplorePage.tsx:15-25` — `useIsMobile` 훅 (768px 기준), 모바일 사이드바 토글
- `QueryPage.tsx:206` — `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` 반응형 데모 질문
- `ReviewPage.tsx:305` — `window.confirm()` 네이티브 확인 대화상자 (Toast 대신)
- `DocumentsPage.tsx:142-145` — 빈 상태 안내 텍스트

**3. 강점**
- 모든 페이지에 에러/로딩 상태 구현 (단일 페이지도 빠짐없음)
- QueryPage 접근성 우수 (role, aria-checked, aria-busy, aria-label 완비)
- shadcn/ui 기반 일관된 디자인 시스템
- Lazy loading + Suspense로 초기 로드 최적화 (App.tsx:8-10)
- ReviewPage 성공 메시지 자동 소멸

**4. 약점**
- 중앙 Toast/Notification 없음 — 인라인 alert + window.confirm() 사용
- UploadPage, ReviewPage, DocumentsPage 테이블이 모바일 미대응 (grid-cols 고정)
- ExplorePage, DocumentsPage에 aria 속성 부족
- 빈 상태 처리가 단순 텍스트 — 가이드/CTA 버튼 없음

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| SHOULD | Toast 라이브러리 도입 (sonner or shadcn/toast) | 전체 페이지 |
| SHOULD | 테이블 반응형 (모바일 카드뷰 or 스크롤) | `UploadPage`, `ReviewPage`, `DocumentsPage` |
| COULD | 빈 상태 컴포넌트 표준화 (아이콘 + 가이드 텍스트 + CTA) | `components/ui/EmptyState.tsx` (신규) |
| COULD | ExplorePage, DocumentsPage aria 속성 보강 | 해당 컴포넌트 |

---

### 축 7: 확장성 & 유지보수 — 77점 (B) | HIGH

**1. 현재 수준**
새 도메인 추가 시 6개 파일 수정 필요 (레지스트리 패턴 3개 + 조건분기 2개 + 골든 데이터 1개). Docstring 95% 커버리지, TODO/FIXME 0개. TypeScript-Pydantic 수동 동기화.

**2. 근거**
- `ontology/domain_hints.py:267-310` — `_DOMAIN_REGISTRY` 리스트에 DomainHint 등록 (레지스트리 패턴)
- `owl/axiom_registry.py:53-57` — `_AXIOM_REGISTRY` dict에 도메인별 axiom 등록 (레지스트리 패턴)
- `evaluation/auto_golden.py:31-41` — `is_ecommerce_domain()` 하드코딩 조건분기
- `data_generator/generator.py:17-50` — 도메인별 테이블 세트 감지 함수 하드코딩
- `frontend/src/types/ontology.ts:1` — `// Mirrors backend Pydantic models` (수동 동기화)
- 모든 라우터: `prefix="/api/v1/*"` — v2 전략 없음

**3. 강점**
- domain_hints, axiom_registry, template_registry: 레지스트리 패턴 (O(1) 추가)
- Docstring ~95% 커버리지 (주요 서비스 함수 10/10 확인)
- TODO/FIXME/HACK 0개 — 기술 부채 인지 관리 우수
- 데드코드 없음 — 구버전 templates.py, executor.py 이미 삭제

**4. 약점**
- 새 도메인 추가 시 6개 파일 변경 필요 (그 중 2개는 조건분기)
- TypeScript 타입 수동 동기화 — 스키마 드리프트 위험
- API v2 전략 없음 (v1 고정)
- `config.py:41` 모델명 하드코딩 (`claude-sonnet-4-20250514`)

**5. 구체적 개선 항목**

| 구분 | 항목 | 위치 |
|------|------|------|
| SHOULD | OpenAPI → TS 자동 생성 (openapi-typescript) | `frontend/scripts/` (신규) |
| SHOULD | 도메인 추가 가이드 문서 (체크리스트) | `docs/DOMAIN_GUIDE.md` (신규) |
| COULD | auto_golden, generator 조건분기 → 레지스트리 패턴 통합 | `evaluation/auto_golden.py`, `data_generator/generator.py` |
| COULD | 모델명 환경변수화 (config에서 하드코딩 제거) | `config.py:41` |

---

## Part 3: 우선순위 로드맵

```
                    높은 임팩트                        낮은 임팩트
              ┌──────────────────────────────┬──────────────────────────────┐
              │      Quick Wins              │      Nice to Have           │
 낮은 노력    │ • pytest-cov + CI 임계값      │ • 빈 상태 컴포넌트 표준화     │
              │ • tenant_id=None 방어         │ • 모델명 환경변수화          │
              │ • 14개 스텁 테스트 정리        │ • aria 속성 보강             │
              │ • 동의어 사전 + 라우터 확장    │ • API v2 네임스페이스        │
              │ • 백업/복구 문서              │                             │
              ├──────────────────────────────┼──────────────────────────────┤
              │      Strategic Bets          │      Deprioritize           │
 높은 노력    │ • LLM 프로바이더 추상 레이어   │ • JWT/OAuth + RBAC          │
              │ • _jobs PG primary 전환       │ • Redis 분산 rate limiting   │
              │ • 프론트엔드 테스트 (Vitest)   │ • 동적 Cypher 생성          │
              │ • Toast 라이브러리 도입        │ • 테이블 모바일 반응형       │
              │ • OpenAPI→TS 자동 생성        │                             │
              │ • 쿼리 LRU 캐시              │                             │
              └──────────────────────────────┴──────────────────────────────┘
```

---

## Part 4: 단계별 실행 계획

### Stage 1: Quick Wins (즉시, 1-2일)

기존 구조 변경 없이 바로 효과가 나는 개선.

| # | 항목 | 수정/생성 파일 | 예상 테스트 |
|---|------|---------------|------------|
| 1 | pytest-cov + CI 커버리지 임계값 60% | `requirements.txt`, `.github/workflows/ci.yml` | +0 (설정만) |
| 2 | tenant_id=None 방어 (빈 문자열 기본값) | `kg_builder/service.py`, `loader.py` | +3 |
| 3 | 14개 스텁 테스트 정리 (구현 or pytest.skip 마킹) | `tests/test_wisdom.py` 외 13개 | +0 (정리) |
| 4 | 동의어 사전 추가 (synonym map) | `query/router_rules.py` | +6 |
| 5 | 백업/복구 가이드 문서 | `docs/BACKUP.md` (신규) | +0 (문서) |
| 6 | 도메인 추가 체크리스트 문서 | `docs/DOMAIN_GUIDE.md` (신규) | +0 (문서) |

**완료 기준 (DoD)**:
- `pytest --cov=app --cov-fail-under=60` CI 통과
- `tenant_id=None`으로 KG 빌드 시 안전한 기본 라벨 사용 확인
- 스텁 파일 0개 (구현 완료 or skip 마킹)
- "주문"/"오더"/"order" 모두 동일 Q1 라우팅 확인
- 신규 테스트 ~9개 통과

---

### Stage 2: Strategic (단기, 3-5일)

아키텍처/품질 수준을 구조적으로 끌어올리는 개선.

| # | 항목 | 수정/생성 파일 | 예상 테스트 |
|---|------|---------------|------------|
| 1 | LLM 프로바이더 추상 레이어 | `llm/provider.py` (신규), `llm_enricher.py`, `router_llm.py`, `local_search.py` | +8 |
| 2 | _jobs PG primary 전환 | `kg_builder/service.py`, `db/kg_build_store.py` | +5 |
| 3 | Toast 라이브러리 도입 (sonner) | `App.tsx`, 6개 페이지, `package.json` | +0 (FE) |
| 4 | 쿼리 결과 LRU 캐시 (TTL 60초) | `query/pipeline.py` | +4 |
| 5 | OpenAPI→TS 자동 생성 스크립트 | `frontend/scripts/generate-types.ts`, `package.json` | +0 (빌드) |
| 6 | 노드+관계 단일 트랜잭션 래핑 | `kg_builder/loader.py:324-390` | +3 |

**완료 기준 (DoD)**:
- LLM 프로바이더 교체가 config 1줄로 가능 (`settings.llm_provider = "anthropic"`)
- 서버 재시작 후에도 진행 중 빌드 잡 복구 가능
- 프론트엔드 에러가 Toast로 표시 (alert/window.confirm 제거)
- 동일 쿼리 2회째 실행 시 캐시 히트 확인
- `npm run generate-types`로 TS 타입 자동 갱신
- 신규 테스트 ~20개 통과

---

### Stage 3: 프로덕션 준비 + 확장 (중기, 1-2주)

실제 배포 및 장기 운영을 위한 마무리.

| # | 항목 | 수정/생성 파일 | 예상 테스트 |
|---|------|---------------|------------|
| 1 | 프론트엔드 테스트 (Vitest + Testing Library) | `frontend/vitest.config.ts`, `frontend/__tests__/` | +20 |
| 2 | E2E 실패 시나리오 테스트 | `tests/test_e2e_failures.py` (신규) | +8 |
| 3 | 테이블 모바일 반응형 | `UploadPage`, `ReviewPage`, `DocumentsPage` | +0 (FE) |
| 4 | auto_golden/generator 레지스트리 패턴 통합 | `evaluation/auto_golden.py`, `data_generator/generator.py` | +4 |
| 5 | CI 커버리지 리포트 + 배지 | `.github/workflows/ci.yml` | +0 (CI) |
| 6 | 빈 상태 + aria 속성 보강 | `components/ui/EmptyState.tsx`, 각 페이지 | +0 (FE) |

**완료 기준 (DoD)**:
- 프론트엔드 `npm run test` 통과 (20+ 테스트)
- Neo4j 다운 / LLM rate limit 시나리오 E2E 검증
- 모바일 768px에서 테이블 가독성 확보
- 새 도메인 추가 시 변경 파일 4개 이하 (현재 6개)
- CI에서 커버리지 리포트 자동 생성 + README 배지
- 신규 테스트 합계 ~32개 통과

---

## Part 5: 리스크 & 트레이드오프

### Stage 1 리스크

| 리스크 | 영향 | 대응 |
|--------|------|------|
| 동의어 사전 오탐 (과도한 매칭) | 잘못된 라우팅 | 보수적 시작 (확실한 동의어만), 골든 데이터로 회귀 검증 |
| pytest-cov 60% 임계값에 기존 코드가 미달 | CI 깨짐 | 우선 `--cov-fail-under=50`으로 시작, 점진 상향 |
| **Breaking change**: 없음 | | |
| **건너뛰었을 때 비용**: tenant_id=None 멀티테넌트 데이터 삭제 사고, 커버리지 블라인드 스팟 | | |

### Stage 2 리스크

| 리스크 | 영향 | 대응 |
|--------|------|------|
| LLM 추상 레이어 과도한 엔지니어링 | 복잡성 증가 | 인터페이스 최소화: `call(messages, model, temperature)` 1개 메서드 |
| _jobs PG 전환 시 진행 중 빌드 소실 | 빌드 재시작 필요 | 배포 시 빌드 큐 비움 확인 |
| sonner 번들 크기 | ~2KB gzip | 무시 가능 |
| LRU 캐시 stale 데이터 | 잘못된 답변 | TTL 60초 + KG 빌드 후 캐시 무효화 |
| **Breaking change**: `_jobs` API 호환 유지 필요, LLM import 경로 변경 가능 | | 기존 함수 시그니처 유지, 내부만 교체 |
| **건너뛰었을 때 비용**: LLM 모델 전환 시 3-4개 파일 수동 수정, 워커 스케일아웃 불가 | | |

### Stage 3 리스크

| 리스크 | 영향 | 대응 |
|--------|------|------|
| Vitest 환경 설정 복잡 (ESM + Tailwind) | 셋업 시간 | shadcn/ui 공식 Vitest 가이드 참고 |
| 레지스트리 패턴 통합 시 기존 도메인 회귀 | 골든 데이터 불일치 | 기존 3개 도메인 (ecommerce/education/insurance) 전수 E2E 검증 |
| 모바일 반응형 변경이 데스크톱 레이아웃 영향 | UX 회귀 | 데스크톱 UI 스냅샷 비교 |
| **Breaking change**: auto_golden/generator 리팩터링 시 import 경로 변경 | | 기존 함수명 유지, 내부 디스패치만 교체 |
| **건너뛰었을 때 비용**: FE 리그레션 감지 불가, 새 도메인 추가 진입장벽 높음 | | |

---

## 부록: 정량 요약

| 지표 | 현재 값 | 이전 분석 (v1) | 변동 |
|------|--------|--------------|------|
| Backend LOC | ~14,964 | ~14,960 | ±0 |
| Backend 파일 | 96 | 96 | ±0 |
| 테스트 파일 (활성/스텁) | 24 / 14 | 39 (미분류) | 분류 완료 |
| 테스트 함수 | ~508 | ~508 | ±0 |
| API 엔드포인트 | 36 (14 라우터) | 36 | ±0 |
| 커스텀 Exception | 17 | 17 | ±0 |
| 골든 QA 페어 | 102 (50+52) | 52 | +50 (unstructured 확인) |
| 규칙 라우터 패턴 | 20 | 12 | +8 (확인) |
| Cypher 템플릿 | 28 | 13 | +15 (확인) |
| LLM 리랭커 | Active (2-stage) | Stubbed (오류) | 정정 |
| Dockerfile | Backend + Frontend | 없음 (v1 오류) | 정정 |
| CI/CD | GitHub Actions 3-job | 없음 (v1 오류) | 정정 |
| Rate Limiting | 60req/60s sliding | 없음 (v1 오류) | 정정 |
| 인증 | 3모드 ASGI 미들웨어 | 단일 API 키 (v1 오류) | 정정 |
| 헬스체크 | Neo4j+PG ping+지연측정 | 단순 200 (v1 오류) | 정정 |
| 프론트엔드 테스트 | 0 | 0 | ±0 |
| TODO/FIXME | 0 | 0 | ±0 |

### v1 → v2 분석 차이점

이전 v1 분석은 일부 파일을 직접 읽지 않고 추측하여 여러 항목을 오판:
- 프로덕션 준비도 52점(D) → 73점(B): Dockerfile, CI/CD, rate limiting, 인증, 헬스체크 모두 이미 구현되어 있었음
- LLM 리랭커 "스텁"으로 오인 → 실제로는 2단계(keyword+LLM) active
- 규칙 라우터 12패턴 → 실제 20패턴, 템플릿 13개 → 실제 28개
- 골든 QA 52개 → 실제 102개 (unstructured 52개 누락)

**개선 프롬프트의 "최소 3개 파일:라인 인용" 규칙이 이러한 오판을 방지하는 데 효과적이었음.**
